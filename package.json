{
  "name": "arivippu",
  "version": "1.0.0",
  "description": "Arivippu Campaign Management Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "start": "node start.js"
  }
}
